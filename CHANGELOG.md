# 更新日志

本文件是发布说明的唯一来源。推送 tag 后，GitHub Actions 会自动提取对应版本的章节作为 Release 正文，同时这段文字也可以直接粘贴到脚本猫的更新说明里。

格式约定（改动前请先读）：

- 每个版本一个二级标题，写成 `## v4.3.0`，必须带 `v` 前缀且与 `package.json` 的版本号一致。
- 版本从新到旧排列，条目用无序列表。
- 面向用户写，说清楚「用户能感知到什么变化」，不要写模块名、函数名这类实现细节。
- **发版前必须先在这里补好当前版本的章节**，否则发布流程会直接失败。

## v4.4.0

- 搜题新增 OpenAI Chat Completions、OpenAI Responses、Claude、Gemini、Grok、DeepSeek，以及 sub2api、new-api 等自建中转接口。
- 服务商按四种接口格式归组，同一格式复用请求逻辑，切换和配置关系更清晰。
- 不同 AI 厂商的地址、模型和密钥会分别保存，切换接口时不会互相覆盖；旧版火山引擎配置会自动迁移。
- 配置面板新增模型列表获取与下拉选择，同时保留手动填写；并提供接口连通性测试、失败重试次数和更清晰的地址提示。
- 优化 AI 返回内容的选项提取，并避免在密钥错误、模型错误等不可恢复问题上重复请求。

## v4.3.0

- 重新整理了脚本的打包方式，安装包体积比上一版减小约百分之十一。功能没有变化。
- 修复登录页在部分平台上「重新识别验证码」按钮不显示的问题。这类平台的登录地址采用哈希路由，此前的判断方式识别不到，需要页面跳转一次按钮才会出现。
- 提升捐赠页面收款码的清晰度。

## v4.2

- 新增自动填写账号和密码功能，可在配置面板中统一保存账号和密码，并在英华系和海旗科技登录页自动填充。
- 优化海旗科技章节定位逻辑，优先使用当前激活章节进行判断，减少章节标题重复带来的误判。
- 优化英华系章节定位逻辑，改为优先识别 `a.on` 和 `nodeId`，避免因章节标题不唯一导致的错误跳转。
- 调整视觉模型自动导入逻辑，支持单独开关控制，关闭后仅阻止自动下载，不影响已存在模型的正常识别。
- 补充配置面板顶部提示，提醒修改完配置后记得保存。
