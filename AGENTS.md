# AGENTS.md

## 项目简介

这个项目是一个基于 **ScriptCat / Tampermonkey** 的用户脚本，用于英华学堂相关平台的自动刷课、验证码识别和考试搜题等功能。

## 语言要求

- 与用户沟通、编写说明、整理规范时，**全程使用中文**。
- 代码注释、提交说明和文档内容也尽量使用中文，保持项目表达统一。

## 主要文件

- `main.js`：脚本主体，包含 UI、配置、自动播放、验证码识别、搜题等核心逻辑。
- `README.md`：项目说明、安装方法、功能介绍、常见问题与更新日志。
- `.gitignore`：Git 忽略规则，避免提交依赖、IDE 配置和临时文件。
- `onnx/`：机器视觉相关资源目录，通常包含模型、字符集或引擎文件。

## 开发注意事项

### 1. 修改脚本时
- 尽量保持 userscript 头部信息完整，尤其是 `@match`、`@grant`、`@require` 等字段。
- 不要随意改动资源地址，除非明确知道其用途。
- `main.js` 中很多逻辑与页面选择器、平台适配、验证码模型加载相关，修改前要确认影响范围。

### 2. 修改 UI 时
- 这个项目是浏览器脚本，但仍然要保持界面清晰、简洁、易操作。
- 配置面板、日志面板、按钮文案尽量保持一致风格。
- 涉及状态显示、提示信息、错误信息时，优先保证用户能快速理解。

### 3. 修改说明文档时
- `README.md` 要和脚本实际功能保持一致。
- 如果新增平台、功能或配置项，记得同步更新说明文档。
- 更新日志要写清楚版本变更的重点。

### 4. 提交与忽略规则
- 不要提交 `node_modules/`、`.idea/`、`.iml` 等本地环境文件。
- 如果新增了新的临时文件、缓存目录或构建目录，及时补充到 `.gitignore`。
- 提交信息建议简洁明确，例如：
  - `更新验证码识别逻辑`
  - `优化配置面板`
  - `新增平台适配`

## 平台分类约定

项目中的网址大致分为两类：

- **新 UI**：`haiqikeji` 相关网址，也可称为 **海旗**。
- **旧 UI**：除 `haiqikeji` 以外的所有网址，也可称为 **英华**。

后续在分析、修改或讨论平台适配时，统一按这个分类来称呼，避免混淆。

## 常见修改位置

- 平台适配：`main.js` 中与网址匹配、DOM 选择器、页面流程相关的部分。
- 验证码识别：`main.js` 中与模型加载、图片处理、OCR 调用相关的部分。
- 搜题功能：`main.js` 中与火山引擎接口、题目解析、答案展示相关的部分。
- 配置界面：`main.js` 中 `UIManager`、`ConfigManager` 相关代码。

## 工作建议

1. 修改前先确认需求范围，尽量只改必要部分。
2. 改完后检查是否需要同步更新 `README.md`。
3. 若涉及新资源文件，确认路径和引用是否正确。
4. 尽量保持兼容性，避免影响原有平台适配。

## 备注

如果后续项目结构或工作流变化较大，可以继续补充本文件，让它始终作为该仓库的操作说明。