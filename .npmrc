# 让 npm version 生成的提交信息符合本项目的中文规范。
# %s 会被替换成新版本号，例如执行 npm version minor 得到「发布 v4.4.0」。
# 不加这一条时 npm 默认只写版本号本身（如「4.3.0」），与其余提交风格不一致。
message=发布 v%s

# 注意：本文件会提交进仓库，切勿在此写入任何令牌或密码。
# 私人凭据应放在用户级配置 ~/.npmrc，那个文件不属于本仓库。
